{
  "name": "cinesync-root",
  "private": true,
  "scripts": {
    "dev:all": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" --names \"frontend,backend\" --prefix-colors \"cyan,magenta\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}